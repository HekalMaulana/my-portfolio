import { createProject, updateProject } from "@/actions/projectAction";

const ProjectForm = ({ initialData }: { initialData?: {} }) => {
  return (
    <div>
      <h1>Project Form Page</h1>
    </div>
  );
};

export default ProjectForm;
